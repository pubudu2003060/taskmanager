import { AppHighlightCompletedTodo } from './app-highlight-completed-todo';

describe('AppHighlightCompletedTodo', () => {
  it('should create an instance', () => {
    const directive = new AppHighlightCompletedTodo();
    expect(directive).toBeTruthy();
  });
});
