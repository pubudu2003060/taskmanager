import { DescriptionSlicePipePipe } from './description-slice-pipe-pipe';

describe('DescriptionSlicePipePipe', () => {
  it('create an instance', () => {
    const pipe = new DescriptionSlicePipePipe();
    expect(pipe).toBeTruthy();
  });
});
